# 一、背景
自己最近在学习FJSP问题的相关知识，也在复现相关论文实验，想通过记录自己的学习过程，与大家一起交流学习。文中内容几乎都是个人对一些问题的思考和理解，并非正确甚至仍有很多问题，也欢迎同学提出疑问或指出问题，尤其是在这个领域的大佬，期待向你们多多学习。
对于FJSP问题求解方法和模型会有很多，这次就先放一边,本文想梳理一下关于FJSP问题中一个非常关键的问题，就是FJSP环境涉及到的“决策步“问题。

>本文核心是依据了《Flexible Job Shop Scheduling via Graph Neural  Network and Deep Reinforcement Learning》这篇论文复现过程中，对整个FJSP环境的表达形式
## FJSP问题
FJSP的全称**Flexible Job Shop Scheduling Problem**柔性作业车间调度问题
问题描述起来并不复杂：当前有很多作业任务Job，每个作业都有一些序列化的工序操作Operation，需要一个接一个的通过机器Machine来加工完成，每个工序都会有自己的加工机器，也就是这些工序只能通过对应的可用机器进行加工，完成这个机器和工序的匹配，就称之为调度，核心就三个对象：
- Job - 作业 - job
- Operation - 工序 - op
- Machine - 机器 - ma

![FJSP问题.png](https://gitee.com/caihh2018/img/raw/master/FJSP问题.png)

通常对于这个环境会建立为马尔科夫模型，这时会有个问题，模型中的 $t$ 实际上是进行调度的决策点，那么，什么时候需要进行调度？
显然是当有机器空闲且有其可加工的工序时，那这个可调度点，就是整个问题的决策步。
这里还需要强调出另外一个关键问题，就是时刻，**时刻≠决策步**，时刻是系统的一个状态属性并非模型之中的 $t$ ，而模型中的 $t$ 则是调度点，可能一个时刻会有多个调度 $t$，同时也可能能经历多个时刻，才会有一次调度 $t$，那为了区分调度的时间点 $t$ 和 时刻，我们后续都称调度 $t$ 为决策步，后面也会不断提到这两个概念。
我们可以画两个时间轴，这样更容易理解时刻和决策步的情况，O-M对，就是调度的op和ma

![决策步与时刻.png](https://gitee.com/caihh2018/img/raw/master/决策步与时刻.png)

## 决策步
**决策步**，是整个FJSP环境的一个细节，从描述来看，根据调度的情况，把时间点推进到对应可进行调度决策的时间点，从而进行调度，这一步就是决策步。从事件触发的角度，大概分成两个部分：
- 初始决策步及首次调度
- 循环推进决策步和时刻
从概念上描述这个问题，似乎是很简单的，但是整个FJSP问题涉及到很多状态的变化，尤其是对于RL求解这个问题，需要将整个问题进行**张量化**表达，就会涉及到非常多的细节，这也是本文想记录的重点。
# 二、初始决策步及首次调度
初始决策步，就是在环境初始化后，决策步时的状态
针对于FJSP问题，初始的决策可以说是"最富裕的仗"：在初始决策步的时刻，恒定为0，这时候所有机器都可用，所有的工序都可加工。
其实这时，是隐藏了一个问题：在0决策步，可能会有多次调度，是一次性调度结束，还是每一个决策步，只进行一次调度呢？我个人的理解是偏向于后者，只需要step步进，时刻维持0即可。同理，在后续的决策步中，也会出现同一个时刻，可能会有多次调度决策，一样维持时刻，只进行推进决策步，这样也就有了上面对于决策步和时刻推进的示意图的理解。我们更进一步，把从0时刻、0决策步发生的流程可以表示出来
![初始决策步.png](https://gitee.com/caihh2018/img/raw/master/初始决策步.png)

即便如此，由于调度的发生，很多状态都需要进行变化，所以需要考虑的问题也会有很多细节。
## 0决策步发生了什么
在步进之前，环境进行初始化，之后：
- 第1步：所有的状态都是初始化状态，状态特征输入给model，会给出一个aciton，action之中会包含（op， ma， job），通过文献模型可知，最终输出的实际上是所有动作的概率分布，所以最终选取的应该是其中一个，不可能有多个决策组合的情况。
- 第2步：根据（op， ma， job）进行调度。在物理环境中，调动就是给机器安排活，但在我们的虚拟环境中，实际上调度就是更新各个张量值。这里暂不展开说，这部分可以专门写篇文章。但是会有些状态张量会在第3步中用到，所以在后面会进行说明。
- 第3步：推进下一个决策步，判断是否仍有可调度的选择。
	- 这部分比较重要，我们在接下来的一节来说
## 推进下一个决策步
首先明确，到底要推进到哪个决策步，这本质就是对于决策步的定义，何时需要做决策。这一点倒是比较确定的：就是当前状态下，当前时刻的可加工工序中，有可用的空闲机器。
接下来进行这一问题的拆解和整理：
### 可用job
对于每一个job，在当前时刻，都会有三个状态
- 加工中（当前有一个op正在加工）
- 加工完成（最后一个op也已完成）
- 待加工（最新的未加工op未在操作中）
或者可以在细分成两类
- 待加工
- 已加工
整体可以用一个向量表达，位置对应索引job，0-1对表示job是否**待加工**

![job状态张量.png](https://gitee.com/caihh2018/img/raw/master/job状态张量.png)

### 可用ma
对于每一台机器，在当前时刻，会有两种状态
- 忙碌
- 空闲
整体可以用一个向量表达，位置对应索引ma，0-1对表示ma是否**空闲**
表示为**当前加工中的ma张量**

![ma状态张量.png](https://gitee.com/caihh2018/img/raw/master/ma状态张量.png)

### op加工时间
这里又有一个隐藏的问题，就是op如何关联到可用job和可用ma
实际上，是从张量上进行的处理和操作：
首先，我们有一个**每个job下个待加工op的索引张量**，核心是因为每一个job当前只有且只有一个op索引。

![待加工张量.png](https://gitee.com/caihh2018/img/raw/master/待加工张量.png)

此外，我们还有一个**所有op与所有ma的加工时间邻接张量（矩阵）**，矩阵的行、列分别是对应op的索引和ma的索引，矩阵的值就是加工时间。

![加工时间邻接张量.png](https://gitee.com/caihh2018/img/raw/master/加工时间邻接张量.png)

这样，我们可以直接通过对**索引张量**直接查询**加工时间邻接张量**，就得到了**每一个job接下来的op的加工时间张量**。

![job-ma加工时间邻接张量.png](https://gitee.com/caihh2018/img/raw/master/job-ma加工时间邻接张量.png)

### 整合
我们可以分情况来看
1、如果当前状态，job均未在待加工状态，此时无可用调度，则需要推进时刻
2、如果当前状态，ma均未在空闲状态，此时同样无可用调度，则需要推进时刻
3、如果当前有可用job，也有可用ma，这时候就要去看，**job当前索引的待加工op在ma上的加工时间**，并求和，如果大于0，就说明在可用job下的可用ma仍然有可加工的op，所以只需要推进决策步，而不需要推进时刻
这里，因为每一个job索引下只对应一个op，其实此时op维度就是job的维度，所以对应张量的位置，就是这个job对应op的加工时间，只需要在叠加判定可用job和可用ma，即可做全局判定，是否需要推进决策步。

![求和加工时间推进决策步.png](https://gitee.com/caihh2018/img/raw/master/求和加工时间推进决策步.png)
另外，如果所有的job全部完成，那么也不需要推进时刻，正常进行计算后续数据，在环境外判定会整个实例调度完成
最后一种情况，就是如果**job当前索引op在ma上的加工时间为0**，也就是此时已经没有可用的O-M对了，就既要推进时刻，直到可以推进决策步
# 三、循环推进决策步和时刻

因为这一部分，也有可能在首次调度后发生，更一般的是在中间调度过程中，不断循环判定是否还有剩余的0-M对，所以单独作为一个部分来进行说明。在初始化结束后，即进入迭代状态

![循环推进决策步和时刻.png](https://gitee.com/caihh2018/img/raw/master/循环推进决策步和时刻.png)

理解这部分的关键，其实需要彻底搞清楚，推进到哪个时刻，又对什么状态产生影响。
直观来说，推进到的时刻点，是机器从忙碌状态变为空闲状态的那个时刻，实际上就是机器的可用时间，所以
- 首先要取的，就是机器可用时间大于当前时刻的机器（当然也存在小于当前时刻的机器，但因为其并没有可用的工序进行操作），之后对这些时间取最小即可。
- 更新推进到的时刻
- 取出推进时刻后**切换状态的机器**

![image.png](https://gitee.com/caihh2018/img/raw/master/20251018113232942.png)

- 更新**机器特征矩张量**中的的机器状态特征，置零
- 更新**机器特征矩张量**中的的机器利用率特征
- 找出涉及到本次调度的job
- 更新**当前加工中的job张量**对应job置零
- 更新**当前加工中的ma张量**对应ma置零
- 更新**当前完成job张量**对应job为true

这里我们还需要再重复提醒一下
以上还需要分成两种情况，在推进到时刻之后，是可能推进决策步，也可能只推进时刻
区别也很明显，如果推进时刻后，
- 有可以进行的调度，则推进决策步
- 否则继续推进时刻
所以我们可以区分这两种情况来看，循环推进都影响了什么
## 推进时刻，不推进决策步
出现这种情况，就说明推进时刻之后，发现仍没有可用的O-M对
我们先来总结，推进决策步什么发生了变化
1. 当前时刻
2. 到达可用时间的机器的状态、利用率
3. 当前加工中的job标签
4. 当前加工中的ma标签
5. 当前完成的job标签
然后再看，判定是否有可用O-M对，用到了哪些变化
- 首先此时没有调度，所以当前job的待调度op不发生变化
- 但job状态，是否在加工中会发生变化，打开了可用job
- 同时ma也进行了释放，打开可用ma
![image.png](https://gitee.com/caihh2018/img/raw/master/20251018114345617.png)

此时，重新判定可用ma和可用job的选择就变多了，进而对于可操作的0-M对就变多了
所以对于整个系统，在仅推进时刻过程中，只需要更新以上信息即可
如果持续未有可用0-M对，则不断推进时刻，直到出现可用0-M对
## 推进时刻，后推进决策步
很显然，这种情况就是，在推进决策步，释放ma和job之后，有了可用的O-M，则可以推进决策步，进而根据状态做出行动，再进行调度，完成了整个决策步的迭代。
这一部分的核心仍然是对0-M对的判定，就进入了循环判定决策步，上面已经讲过，这里就不重复说明了。

# 四、总结
本文其实是从FJSP环境中对于决策步相关的问题出发，按照决策步的顺序以及不同情况，进行分类讨论说明。
本文最核心的内容是在决策步和时刻推进过程中，到底涉及到了那些状态信息的变化，其实对于FJSP问题，会涉及到很多张量，循环不断回答这两个问题：
- 时刻需不需要推进
- 决策步需不需要推进
这两个问题的时候，并不需要全部的状态更新，仅需要考虑
- 当从0时刻、0决策步出发，此时必定发生决策步推进，否则整个实例后续都不会进行调度了
- 决策步推进，更新：
	- 发生调度的状态变化，这一部分本文未涉及全貌，主要是跟决策步和时刻步推进关联不大
	- 调度后判定可用O-M对，这部分是核心
	- 只要有可用0-M，就利用模型做决策，输入环境结果，进入推进决策步循环
- 当没有可用O-M，则进入判定推进时刻循环
	- 核心仍然是在推进时刻之后，更新：
		- 当前可用ma、可用job
		- 根据邻接加工时间判定是否有可用调度，否则继续推进时刻
最终，我们从全局来看，就是如下图逻辑：

![image.png](https://gitee.com/caihh2018/img/raw/master/20251018120942088.png)
